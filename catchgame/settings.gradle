pluginManagement {
    repositories {
        google()
        maven { url 'https://cache-redirector.jetbrains.com/repo.maven.apache.org/maven2' }
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        maven { url 'https://cache-redirector.jetbrains.com/repo.maven.apache.org/maven2' }
        mavenCentral()
    }
}
rootProject.name = "CatchGame"
include ':app'
